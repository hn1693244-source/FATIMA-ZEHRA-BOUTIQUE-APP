# Configuration

Configuration files for the application:

- **config.yaml** - Main configuration (environment variables override)
- **env/** - Environment-specific configs
  - dev.env.example
  - staging.env.example
  - prod.env.example

Copy to `.env` and customize for your environment.
