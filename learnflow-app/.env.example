# ============================================================================
# LearnFlow App - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git (it's in .gitignore)
#
# For development:  cp .env.example .env
# For staging:      cp config/env/staging.env.example config/.env
# For production:   cp config/env/prod.env.example config/.env
#
# ============================================================================

# ============================================================================
# DATABASE
# ============================================================================

# Option 1: Local PostgreSQL (docker-compose postgres)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/learnflow

# Option 2: Neon PostgreSQL (cloud)
# Get from: https://console.neon.tech
# Format: postgresql://[user]:[password]@[host].neon.tech/[dbname]
DATABASE_URL=postgresql://user:password@localhost:5432/learnflow

# Local database credentials (if using docker-compose postgres)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=learnflow

# ============================================================================
# AUTHENTICATION
# ============================================================================

# JWT Secret (generate random 32+ character string)
# Generate with: openssl rand -hex 16
JWT_SECRET=your-random-32-character-secret-key-here-change-this

# JWT Token expiration (hours)
JWT_EXPIRATION=24

# ============================================================================
# AI INTEGRATION
# ============================================================================

# AI Model to use: openai, gemini, goose, custom
AI_MODEL=openai

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Gemini API Key (optional)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Goose API Key (optional)
GOOSE_API_KEY=your-goose-api-key-here

# ============================================================================
# FRONTEND
# ============================================================================

# Public API URL (visible to browser)
NEXT_PUBLIC_API_URL=http://localhost:8001

# Site name (for branding)
NEXT_PUBLIC_SITE_NAME=Fatima Zehra Boutique

# ============================================================================
# BACKEND
# ============================================================================

# Backend API URLs (internal, for service-to-service communication)
USER_SERVICE_URL=http://user-service:8000
PRODUCT_SERVICE_URL=http://product-service:8000
ORDER_SERVICE_URL=http://order-service:8000
CHAT_SERVICE_URL=http://chat-service:8000

# ============================================================================
# ENVIRONMENT
# ============================================================================

# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode (true/false)
DEBUG=true

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# ============================================================================
# CORS & SECURITY
# ============================================================================

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8001,http://localhost:8002,http://localhost:8003,http://localhost:8004

# Allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# EMAIL (Optional - for Phase 2)
# ============================================================================

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@fatima-zehra-boutique.com

# ============================================================================
# PAYMENT GATEWAY (Optional - for Phase 3)
# ============================================================================

# Stripe
STRIPE_API_KEY=sk_test_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# PayPal
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_CLIENT_SECRET=your-paypal-secret

# ============================================================================
# CLOUD DEPLOYMENT (Optional - for Production)
# ============================================================================

# AWS
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# Google Cloud
GOOGLE_CLOUD_PROJECT=your-gcp-project
GOOGLE_CLOUD_KEY_FILE=path/to/service-account.json

# Azure
AZURE_SUBSCRIPTION_ID=your-azure-subscription
AZURE_RESOURCE_GROUP=your-resource-group

# ============================================================================
# DEPLOYMENT
# ============================================================================

# Docker registry (for CI/CD)
DOCKER_REGISTRY=ghcr.io
DOCKER_USERNAME=your-github-username
DOCKER_PASSWORD=your-github-token

# Kubernetes
KUBERNETES_NAMESPACE=default
KUBERNETES_INGRESS_HOST=example.com

# Netlify (for serverless functions)
NETLIFY_SITE_NAME=your-site-name
NETLIFY_AUTH_TOKEN=your-netlify-token

# ============================================================================
# MONITORING & LOGGING (Optional)
# ============================================================================

# Sentry (error tracking)
SENTRY_DSN=https://your-sentry-dsn

# DataDog
DATADOG_API_KEY=your-datadog-api-key

# New Relic
NEW_RELIC_LICENSE_KEY=your-new-relic-key

# ============================================================================
# THIRD-PARTY SERVICES
# ============================================================================

# SendGrid (email)
SENDGRID_API_KEY=your-sendgrid-key

# Twilio (SMS)
TWILIO_ACCOUNT_SID=your-twilio-account
TWILIO_AUTH_TOKEN=your-twilio-token
TWILIO_PHONE_NUMBER=+1234567890

# AWS S3 (file storage)
AWS_S3_BUCKET=your-s3-bucket
AWS_S3_REGION=us-east-1

# ============================================================================
# NOTES
# ============================================================================
#
# DO NOT commit this file if it contains real credentials
# Always use .env.example as template
# Rotate secrets regularly in production
# Use different secrets for each environment
# Store production secrets in secure secret manager:
#   - AWS Secrets Manager
#   - Netlify Environment Variables
#   - GitHub Secrets
#   - Kubernetes Secrets
#
# ============================================================================
